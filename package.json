{
  "name": "dafghif-learning-portfolio",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.13",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "serve": "vite preview --host 127.0.0.1",
    "generate:portfolio-ai": "bun scripts/generate-portfolio-ai-context.mjs",
    "lint": "bun scripts/validate-site.mjs",
    "test": "bun scripts/validate-site.mjs",
    "validate": "bun scripts/validate-site.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
